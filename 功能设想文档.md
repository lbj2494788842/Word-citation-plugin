关于该插件的设想：

功能流程：

    在word中点开该插件，于是该插件就会展开。首先有一个空白的输入框。让用户上传引用文献的纯文本模式。暂定只能复制进去，添加一个一键复制按钮。用户复制好内容后，就会按照行数自动分段（分段时自动去掉每一段最前面的[]以及其中的数字）（其实已经会分好），并变成一个一个选项。于是用户只需要在对应的word的文本中移动光标，点击到合适的位置后，再点击右侧插件页面内根据用户输入的内容所生成的选项，就会自动在该位置插入脚注，并且自动将所选的选项以纯文本的形式注入到脚注的内容中。
